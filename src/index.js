import React from 'react';
import ReactDOM from 'react-dom';
import About from './screens/About';// Adjust the path and file name if necessary


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <About />
  </React.StrictMode>
);
